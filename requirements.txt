# Core AI / structured output
google-genai>=1.0.0
pydantic>=2.5.0

# Gmail API (read + send)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Financial data — async HTTP client replacing synchronous yfinance
httpx>=0.27.0

# PDF generation (after install: playwright install chromium)
playwright>=1.40.0

# Async file I/O
aiofiles>=23.2.0

# Financial Modeling Prep SDK (economic + earnings calendar)
fmpsdk>=0.5.0

# Google Cloud Storage (HTML briefing hosting)
google-cloud-storage>=2.16.0

# Environment variable management
python-dotenv>=1.0.0

# Terminal output — rich progress bar and coloured logging
rich>=13.0.0
